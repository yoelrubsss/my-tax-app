import { prisma } from './lib/prisma'; async function main() { console.log('Updating to 18%...'); const txs = await prisma.transaction.findMany(); for (const t of txs) { const newVat = parseFloat((t.amount * 0.18 / 1.18).toFixed(2)); const newNet = parseFloat((t.amount - newVat).toFixed(2)); let recognized = t.recognizedVatAmount; if (t.type === 'income') { recognized = newVat; } else if (t.vatAmount > 0) { recognized = parseFloat((t.recognizedVatAmount / t.vatAmount * newVat).toFixed(2)); } await prisma.transaction.update({ where: { id: t.id }, data: { vatRate: 0.18, vatAmount: newVat, netAmount: newNet, recognizedVatAmount: recognized } }); } console.log('Success! All transactions are now 18%'); } main().catch(console.error)
